# PR Summary Lite

Generate a Markdown pull request summary from local git diffs.

```bash
node ./bin/pr-summary-lite.js main
node ./bin/pr-summary-lite.js HEAD~1 > PR_SUMMARY.md
```

Use it locally, in GitHub Actions, or before asking an AI reviewer to inspect a change.
